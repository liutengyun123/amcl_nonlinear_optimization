# Third-Party Notices

This package vendors a subset of the AMCL core algorithm implementation from
`navigation2/nav2_amcl` for local use inside `localization_ndt`.

Vendored components:

- `include/nav2_amcl/**`
- `src/nav2_amcl_vendor/**`

Upstream source:

- Navigation2 `nav2_amcl`

Relevant upstream licenses:

- `LGPL-2.1-or-later` for the AMCL core sources derived from Player / ROS AMCL
- `Apache-2.0` for `portable_utils.hpp`

Original license headers were preserved in the vendored files.
